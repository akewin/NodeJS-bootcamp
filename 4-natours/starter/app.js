const express = require("express");
// defining app as express and port
const app = express();
const port = 3000;

app.port(';')

app.listen(port, () => {
    console.log(`App running on port ${port}`);
})